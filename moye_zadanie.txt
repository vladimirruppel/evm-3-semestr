Y:= P(S) - P(T) +P^2(S-T) - P^3(1), где P(x)=ax^2 - bx + 16;

Программные прерывания:
1. для ввода исходных данных
2. для вывода результатов

Аппаратные прерывания
1. изменение значений А и B  в подпрограмме и рестарт программы с очисткой стека и рабочих переменных(другие значения остаются неизменными);
2. вывод значений из вершины стека, если прерывание произошло в подпрограмме, и продолжение работы программы. 